const chai = require('chai');
const expect = chai.expect;

describe('GitLab Issues', () => {
    describe('Issues', () => {
      it('Issue 1', async () => {
          expect("toimii").to.equal("toimii");
      });
      it('Issue 2', async () => {
          expect("toimii").to.equal("toimii");
      });
      it('Issue 3', async () => {
          expect("toimii").to.equal("toimii");
      });
      it('Issue 4', async () => {
          expect("toimii").to.equal("toimii");
      });
      it('Issue 5', async () => {
          expect("toimii").to.equal("toimii");
      });
      it('Issue 6', async () => {
          expect("toimii").to.equal("toimii");
      });
      it('Issue 7', async () => {
          expect("toimii").to.equal("toimii");
      });
      it('Issue 8', async () => {
          expect("toimii").to.equal("toimii");
      });
      it('Issue 9', async () => {
          expect("toimii").to.equal("toimii");
      });
      it('Issue 10', async () => {
          expect("toimii").to.equal("toimii");
      });
    });
  });